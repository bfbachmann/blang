extern {
    fn exit(code: u64)
    fn printf(msg: str) ~ u64
    fn memcpy(dest: rawptr, src: rawptr, n_bytes: u64) ~ rawptr
    fn malloc(nbytes: u64) ~ rawptr
}

fn assert(cond: bool) {
    if !cond {
        exit(1)
    }
}