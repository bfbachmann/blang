extern fn puts(msg: str): uint
extern fn read(fd: uint, buf: *u8, size: uint): uint
extern fn fflush(fd: uint)
extern fn write(fd: uint, ptr: *u8, nbytes: uint)

const stdin: uint = 0
const stdout: uint = 1
const stderr: uint = 2
