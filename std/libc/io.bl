extern {
    fn puts(msg: str): u32
    fn read(fd: u32, buf: *u8, size: u64): u64
    fn fflush(fd: u32)
    fn write(fd: u32, ptr: *u8, nbytes: u64)
}

const {
    STDIN: u32 = 0
    STDOUT: u32 = 1
    STDERR: u32 = 2
}
