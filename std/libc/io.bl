extern {
    fn puts(msg: str): uint
    fn read(fd: uint, buf: *u8, size: uint): uint
    fn fflush(fd: uint)
    fn write(fd: uint, ptr: *u8, nbytes: uint)
}

const {
    stdin: uint = 0
    stdout: uint = 1
    stderr: uint = 2
}
